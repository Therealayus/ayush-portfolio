declare module "gsap-trial/SplitText" {
  // This project uses gsap-trial's SplitText plugin.
  // The library doesn't ship types, so we keep it as `any` to unblock builds.
  export const SplitText: any;
}

